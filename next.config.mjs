/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['gaseous-merideth-unvenomous.ngrok-free.dev'],
};

export default nextConfig;
